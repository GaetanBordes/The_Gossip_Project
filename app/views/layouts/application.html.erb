<!DOCTYPE html>
<html>
  <head>
    <title>The Gossip Project</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm py-3">
        <div class="container">
          <a class="navbar-brand fw-bold" href="/">The Gossip Project</a>
          <div class="d-flex justify-content-between w-100">
            <div class="d-flex">
              <a class="nav-link link-box mx-2" href="<%= root_path %>">Accueil</a>
              <a class="nav-link link-box mx-2" href="<%= team_path %>">Équipe</a>
              <a class="nav-link link-box mx-2" href="<%= contact_path %>">Contact</a>
            </div>

            <!-- Section pour les liens de connexion/inscription ou déconnexion -->
            <div class="d-flex">
              <% if user_signed_in? %>
                <a class="nav-link link-box mx-2" href="<%= user_path(current_user) %>">Mon Profil</a>
                <%= form_for(:user, url: destroy_user_session_path, method: :delete, html: { class: 'd-inline' }) do |f| %>
                  <%= f.submit "Se déconnecter", class: "nav-link btn btn-link" %>
                <% end %>
              <% else %>
                <a class="nav-link link-box mx-2" href="<%= new_user_session_path %>">Se connecter</a>
                <a class="nav-link link-box mx-2" href="<%= new_user_registration_path %>">S'inscrire</a>
              <% end %>
            </div>
          </div>
        </div>
      </nav>
    </header>

    <!-- Contenu de chaque page -->
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
